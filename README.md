# Expense Tracker API

A FastAPI backend with JWT authentication and PostgreSQL database.

## Features
- Register & login with secure password hashing (bcrypt)
- JWT-based authentication
- Create and list user expenses
- PostgreSQL + SQLAlchemy ORM

## Running Locally
\`\`\`bash
uvicorn app.main:app --reload
\`\`\`
